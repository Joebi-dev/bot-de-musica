/* import { Player } from '../structure/Player'

interface _players {
    [key: string]: Player
}
//Amarzenar os players que estao tocando
var players: _players = {}

export function getPlayerByGuildID(guildId: string): Player | null {
    return players[guildId]
}
export function setPlayerByGuildID(guildId: string, player: Player) {
    players[guildId] = player
    return player
}

export function clearPlayer(guildId: string) {
    delete players[guildId]
}
 */